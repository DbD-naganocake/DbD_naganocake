<%- if controller_name != 'sessions' %>
  <h4>すでに登録済の方</h4>
  <div class="public-registrations-link">
    <%= link_to "こちら", new_session_path(resource_name) %>からログインして下さい。
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <h4>会員登録がお済でない方</h4>
  <div class="public-session-link">
    <%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行って下さい。
  </div>
<% end %>

