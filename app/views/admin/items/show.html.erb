<h3>商品詳細</h3>


  <table>
    <tbody>
      <tr>
        <td>商品名</td>
        <td><%= @item.item_name %></td>
      </tr>
      <tr>
        <td>商品説明</td>
        <td><%= @item.item_caption %></td>
      </tr>
      <tr>
        <td>ジャンル</td>
        <td><%= @item.genre.genre_name %></td>
      </tr>
      <tr>
        <td>税込み価格（税抜価格）</htdd>
        <td><%= @item.with_tax.to_s(:delimited) %>(<%= @item.without_tax.to_s(:delimited) %>) 円</span></td>
      </tr>
      <tr>
        <td>販売ステータス</td>
        <td>
          <% if @item.sale_status == true %>
            販売中
          <% else %>
            販売中止中
          <% end %>
        </td>
      </tr>
    </tbody>
  </table>